#!/usr/bin/env bash

## Copyright (C) 2020-2026 Aditya Shakya <adi1090x@gmail.com>

## Export Environment Variables and Start Hyprland

## Setting environment variables for wayland session
export XDG_SESSION_TYPE=wayland
export XDG_SESSION_DESKTOP=Hyprland
export XDG_CURRENT_DESKTOP=Hyprland
export XDG_CURRENT_SESSION=Hyprland

## GTK environment
export TDESKTOP_DISABLE_GTK_INTEGRATION=1
export CLUTTER_BACKEND=wayland
export BEMENU_BACKEND=wayland

## Firefox
export MOZ_ENABLE_WAYLAND=1

## Qt environment
export QT_QPA_PLATFORMTHEME=qt6ct
export QT_AUTO_SCREEN_SCALE_FACTOR=1
export QT_QPA_PLATFORM="wayland;xcb"
export QT_WAYLAND_DISABLE_WINDOWDECORATION=1

## Elementary environment
export ELM_DISPLAY=wl
export ECORE_EVAS_ENGINE=wayland_egl
export ELM_ENGINE=wayland_egl
export ELM_ACCEL=opengl
# export ELM_SCALE=1

## SDL environment
export SDL_VIDEODRIVER=wayland

## Java environment
export _JAVA_AWT_WM_NONREPARENTING=1

# LibreOffice
export SAL_USE_VCLPLUGIN=gtk3

## GDK
export GDK_BACKEND="wayland,x11,*"

export NO_AT_BRIDGE=1
export WINIT_UNIX_BACKEND=wayland

# Re-export DBus session variables
export DBUS_SESSION_BUS_ADDRESS
export DBUS_SESSION_BUS_PID

## NVIDIA Specific (enable if your system has Nvidia GPU)
#if lspci -k | grep -P 'VGA|3D|Display' | grep -qw NVIDIA &&
#   lsmod | grep -qw nvidia; then
#	export GBM_BACKEND="nvidia-drm"
#	export __GLX_VENDOR_LIBRARY_NAME="nvidia"
#	export LIBVA_DRIVER_NAME="nvidia"
#	export __GL_GSYNC_ALLOWED=""
#	export __GL_VRR_ALLOWED=""
#	export AQ_NO_ATOMIC="1"
#fi

## All Virtual machines workarounds (is cause issues)
#if systemd-detect-virt --quiet; then
#    export WLR_NO_HARDWARE_CURSORS=1
#fi

## Virtualbox workarounds
if [[ "$(systemd-detect-virt)" == "oracle" ]]; then
    export WLR_NO_HARDWARE_CURSORS=1
fi

## Start Hyprland
# Small delay to allow the session environment to settle
sleep 0.5
exec /usr/bin/archcraft-session start-hyprland -- --config ~/.config/hyprcat/hyprland.lua
